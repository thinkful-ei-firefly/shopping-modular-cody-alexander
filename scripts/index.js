'use strict';
/* global shoppingList, cuid */

function main() {
  shoppingList.bindEventListeners();
  shoppingList.render();
}

$(main);

// eslint-disable-next-line no-undef
console.log(store);
console.log(foo);
